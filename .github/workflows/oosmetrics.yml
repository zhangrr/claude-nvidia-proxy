name: oosmetrics Health Check
on:
  schedule:
    - cron: '0 9 * * 1'
  workflow_dispatch:
permissions:
  issues: write
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: oosmetrics/health-check-action@v1
        env:
          GITHUB_TOKEN: $
